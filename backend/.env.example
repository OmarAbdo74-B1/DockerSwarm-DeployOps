# postgres://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgres://postgres:postgres@localhost:5432/automarket

# set to "true" if your provider requires SSL (e.g. most managed cloud Postgres)
PGSSL=false

JWT_SECRET=change-me-in-production
PORT=3000
